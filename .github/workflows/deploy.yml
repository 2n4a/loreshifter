name: Deploy application

on:
  push:
    branches: [main]
    paths-ignore:
      - '**/README.md'
      - 'LICENSE'
      - 'docs/**'

jobs:
  db-build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/loreshifter-db
          tags: |
            type=raw,value=latest
            type=raw,value=sha-${{ github.sha }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: db/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  backend-build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/loreshifter-backend
          tags: |
            type=raw,value=latest
            type=raw,value=sha-${{ github.sha }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: LoreshifterBackend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs:
      - db-build-and-push
      - backend-build-and-push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Ansible Playbook to deploy
        uses: dawidd6/action-ansible-playbook@v2
        with:
          directory: deploy
          playbook: deploy-ansible-playbook.yaml
          options: |
            -i "${{ vars.DEPLOY_SERVER_HOST }},"
            -e "github_actor=${{ vars.GHCR_USERNAME }}"
            -e "github_token=${{ secrets.GHCR_TOKEN }}"
            -e "db_password=${{ secrets.DB_PASSWORD }}"
            -e "oauth2_github_client_id=${{ secrets.OAUTH2_GITHUB_CLIENT_ID }}"
            -e "oauth2_github_client_secret=${{ secrets.OAUTH2_GITHUB_CLIENT_SECRET }}"
            -e "gen_api_key=${{ secrets.GEN_API_KEY }}"
            -u "${{ vars.DEPLOY_SERVER_USER }}"
          key: ${{ secrets.DEPLOY_SERVER_KEY }}
          requirements: ansible-requirements.yaml